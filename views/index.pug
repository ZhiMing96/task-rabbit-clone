extends layout 

block content 
    h1 #{title}
    ul.list-group
        each article, i in articles
            li.list-group-item 
                a(href='/articles/' + article.article_id)= article.title

